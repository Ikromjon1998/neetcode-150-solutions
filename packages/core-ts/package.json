{
  "name": "@neetcode/core",
  "version": "1.0.0",
  "private": true,
  "description": "Pure, framework-free NeetCode algorithms in TypeScript. Knows nothing about HTTP.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
