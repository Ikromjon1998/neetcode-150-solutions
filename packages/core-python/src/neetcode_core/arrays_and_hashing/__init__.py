"""Topic 01 — Arrays & Hashing.

The recurring lesson of this topic: a hash map buys you O(1) membership and lookup, which
collapses an O(n^2) scan of every pair into a single O(n) pass. You pay for it in memory.
"""
